var slider = document.querySelector('#slider');

var flkty = new Flickity( slider, {
    cellAlign: 'left',
    contain: true,
    freeScroll: true,
    wrapAround: true,
    autoplay: true,
    pageDots: false,
});